import React from 'react';

// This component is currently unused and was causing build errors. 
// It has been replaced with this stub to allow the build to pass.
const VoteGeneratorAdmin: React.FC = () => {
  return null;
};

export default VoteGeneratorAdmin;